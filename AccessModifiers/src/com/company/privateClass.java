package com.company;

public class privateClass {
    /****
     * Note: Top level: Only classes, Interfaces, and Enums exist at the top Level, Everything else must be included within one of these.
     * 1. Public: the object is visible to all classes everywhere, whether they are in the same package or have imported the package contaioning the
     * public class
     *
     * 2. Package-private: The object is only available within its own package(and it is visible to every class within the same package). Package-private
     * is specified bu not specifying, i.e it is the default if you do not specify pub;ic. There is not a "package-private" Keyword.
     *
     * 3. Top Level -> Member level -> block level ->
     */
}
